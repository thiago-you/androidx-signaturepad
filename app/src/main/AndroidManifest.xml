<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="you.thiago.signaturepad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name="you.thiago.signaturepad.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="you.thiago.signaturepad.ComposeActivity" />
        <activity android:name="you.thiago.signaturepad.ViewActivity" />
        <activity android:name="you.thiago.signaturepad.DataBindingActivity" />
        <activity android:name="you.thiago.signaturepad.SaveRestoreActivity" />
    </application>

</manifest>